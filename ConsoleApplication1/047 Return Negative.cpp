//Create a function that takes a number as an argument and returns negative of that number.
//Return negative numbers without any change.
#include "047 Return Negative.h"
int returnNegative(int n) {
	return n < 0 ? n : -n;
}